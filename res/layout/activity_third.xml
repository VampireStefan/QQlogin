<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ThirdActivity" >

    <EditText
        android:id="@+id/register_pwd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/register_number"
        android:layout_below="@+id/register_number"
        android:layout_marginTop="25dp"
        android:inputType="textPassword"
        android:hint="请输入密码"
        android:ems="10" />

    <Button
        android:id="@+id/register_ackonwledgeReg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/register_acknowledgePwd"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="37dp"
        android:text="确认注册" />

    <EditText
        android:id="@+id/register_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:inputType="number"
        android:hint="请输入账号"
        android:ems="10" />

    <EditText
        android:id="@+id/register_acknowledgePwd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/register_pwd"
        android:layout_below="@+id/register_pwd"
        android:layout_marginTop="26dp"
        android:inputType="textPassword"
        android:ems="10"
        android:hint="确认密码" >

        <requestFocus />
    </EditText>

</RelativeLayout>
